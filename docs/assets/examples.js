window.revealKtExamples = [
  {
    "id": "technical",
    "name": "RetryTalk",
    "description": "A four-slide engineering talk with highlighted Kotlin code, takeaways, and speaker notes.",
    "source": "import dev.limebeck.revealkt.core.RevealKt\nimport dev.limebeck.revealkt.dsl.*\nimport dev.limebeck.revealkt.dsl.slides.*\n\ntitle = \"Reliable retries in Kotlin\"\nconfiguration {\n    theme = RevealKt.Configuration.Theme.Predefined.BLACK\n    slideNumber = RevealKt.Configuration.SlideNumber.Custom(\"c/t\")\n    pdfSeparateFragments = false\n}\nslides {\n    regularSlide {\n        +title { \"Reliable retries in Kotlin\" }\n        +regularText { \"A five-minute engineering talk: retry only what can safely happen twice.\" }\n        +note { \"Ask the audience what happens when a payment request times out after reaching the server.\" }\n    }\n    regularSlide {\n        +title { \"The timeout is ambiguous\" }\n        +unorderedListOf(\"The server may have completed the operation.\", \"Repeating a write can duplicate its effect.\", \"Use an idempotency key for retried writes.\")\n    }\n    regularSlide {\n        +title { \"Keep the policy explicit\" }\n        +code(lang = \"kotlin\", lines = \"1-6\") {\n            \"\"\"\n            fun shouldRetry(status: Int, attempt: Int): Boolean =\n                attempt < 3 && status in setOf(429, 502, 503, 504)\n\n            val retry = shouldRetry(status = 503, attempt = 1)\n            println(retry) // true\n            \"\"\".trimIndent()\n        }\n        +note { \"The example models the decision only. Production code also needs backoff, deadlines and idempotency.\" }\n    }\n    regularSlide {\n        +title { \"Takeaways\" }\n        +unorderedListOf(\"Retry transient failures, not every exception.\", \"Bound attempts and total elapsed time.\", \"Measure exhausted retries and duplicate suppression.\", fragmented = false)\n        +regularText { \"Discussion: which operations in your service are safe to retry?\" }\n    }\n}\n"
  },
  {
    "id": "lesson",
    "name": "NullSafety",
    "description": "A three-slide null-safety lesson with incremental explanations and a practice exercise.",
    "source": "import dev.limebeck.revealkt.core.RevealKt\nimport dev.limebeck.revealkt.dsl.*\nimport dev.limebeck.revealkt.dsl.slides.*\n\ntitle = \"Kotlin null safety: predict, reveal, explain\"\nconfiguration {\n    theme = RevealKt.Configuration.Theme.Predefined.WHITE\n    slideNumber = RevealKt.Configuration.SlideNumber.Enable\n    pdfSeparateFragments = false\n}\nslides {\n    regularSlide {\n        +title { \"What does this print?\" }\n        +code(lang = \"kotlin\") {\n            \"\"\"\n            val name: String? = null\n            println(name?.length ?: 0)\n            \"\"\".trimIndent()\n        }\n        +regularText { \"Think first. Advance one step at a time to explain your answer.\" }\n        +note { \"Give learners 20 seconds before moving to the explanation.\" }\n    }\n    regularSlide {\n        +title { \"Follow the expression\" }\n        +orderedListOf(listOf(\"name contains null.\", \"The safe call ?. skips length and produces null.\", \"The Elvis operator ?: selects 0.\", \"The program prints 0.\"), fragmented = true)\n        +note { \"The first item is visible immediately; each subsequent item is a fragment.\" }\n    }\n    regularSlide {\n        +title { \"Try another input\" }\n        +regularText { \"Change null to Kotlin. Predict the output, then run the code.\" }\n        +unorderedListOf(\"Answer: 6.\", \"The safe call now returns the string length.\", \"The Elvis fallback is not evaluated.\", fragmented = true)\n        +note { \"Ask learners to describe when a nullable value should be rejected instead of receiving a fallback.\" }\n    }\n}\n"
  },
  {
    "id": "custom-theme",
    "name": "BrandDeck",
    "description": "A three-slide presentation with a complete CSS theme and an SVG pipeline diagram.",
    "source": "import dev.limebeck.revealkt.core.RevealKt\nimport dev.limebeck.revealkt.dsl.*\nimport dev.limebeck.revealkt.dsl.slides.*\n\ntitle = \"From a script to a shareable presentation\"\nconfiguration {\n    theme = RevealKt.Configuration.Theme.Custom(\"assets/theme.css\")\n    slideNumber = RevealKt.Configuration.SlideNumber.Custom(\"c/t\")\n    pdfSeparateFragments = false\n}\nslides {\n    regularSlide {\n        +title { \"Write. Preview. Share.\" }\n        +regularText { \"This presentation brings its own CSS theme and SVG image.\" }\n        +note { \"The theme uses system fonts, so no font installation is required.\" }\n    }\n    regularSlide {\n        +title { \"One source, three outputs\" }\n        +img(src = \"pipeline.svg\") { width = 850 }\n        +regularText { \"Keep theme.css and pipeline.svg beside each other in assets/.\" }\n    }\n    regularSlide {\n        +title { \"Make it yours\" }\n        +unorderedListOf(\"Edit the accent color in assets/theme.css.\", \"Save while run is active to see live reload.\", \"Bundle the entire output directory or export a PDF.\", fragmented = false)\n    }\n}\n"
  }
];
